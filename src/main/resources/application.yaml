spring:
  application:
    name: rate-limiter-service

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,metrics

  endpoint:
    health:
      show-details: always


rate-limiting:
  plans:
    free:
      algorithm: TOKEN_BUCKET_REDIS
      capacity: 10
      refill-rate: 10
      refill-period: 1
      time-unit: MINUTES
    premium:
      algorithm: SLIDING_WINDOW_REDIS
      capacity: 20
      refill-period: 1 # This is window size
      time-unit: MINUTES